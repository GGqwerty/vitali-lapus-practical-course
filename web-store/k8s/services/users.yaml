apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-web-store
  namespace: inno
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users
  template:
    metadata:
      labels:
        app: users
    spec:
      containers:
        - name: users
          image: docker-users-web-store:latest
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 60
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 5
            timeoutSeconds: 60
            failureThreshold: 10
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: docker
          imagePullPolicy: IfNotPresent
---
apiVersion: v1
kind: Service
metadata:
  name: users-web-store
  namespace: inno
spec:
  ports:
    - port: 8080
  selector:
    app: users
