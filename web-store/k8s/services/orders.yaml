apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-web-store
  namespace: inno
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orders
  template:
    metadata:
      labels:
        app: orders
    spec:
      containers:
        - name: orders
          image: docker-orders-web-store:latest
          ports:
            - containerPort: 8083
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8083
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 60
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8083
            initialDelaySeconds: 60
            periodSeconds: 5
            timeoutSeconds: 60
            failureThreshold: 10
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: docker
          imagePullPolicy: IfNotPresent
---
apiVersion: v1
kind: Service
metadata:
  name: orders-web-store
  namespace: inno
spec:
  ports:
    - port: 8083
  selector:
    app: orders
